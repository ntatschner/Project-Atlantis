<Window x:Class="Atlantis.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Atlantis"
        mc:Ignorable="d"
        WindowStyle="None"
        AllowsTransparency="True"
        WindowStartupLocation="CenterScreen"
        Title="Project Atlantis" Height="600" Width="900">

    <Window.Resources>
        <Style TargetType="{x:Type local:MainWindow}">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Window}">
                        <!-- Outer border with the drop shadow margin -->
                        <Border Padding="{Binding OuterMarginSizeThickness, FallbackValue=5}">
                            
                            <!-- Main Windows outline -->
                            <Grid>
                                
                                <!-- Main window and drop shadow-->
                                <Border CornerRadius="{Binding WindowCornerRadius, FallbackValue=5}"
                                        Background="{Binding ForegroundMainLight, FallbackValue=White}">
                                    <Border.Effect>
                                        <DropShadowEffect ShadowDepth="0" Opacity="0.5" />
                                    </Border.Effect>
                                </Border>
                                    
                                <!-- The main window content -->
                                <Grid >
                                    <Grid.RowDefinitions>
                                        
                                        <!-- Title Bar-->
                                        <RowDefinition Height="{Binding TitleHeightGridLength, FallbackValue=35}"/>
                                        <!-- Drop Shadow -->
                                        <RowDefinition Height="Auto"/>
                                        <!-- Window Content -->
                                        <RowDefinition Height="*"/>
                                    </Grid.RowDefinitions>
                                    
                                    <!-- Title Bar -->
                                    <Grid Grid.Column="0" Panel.ZIndex="1">
                                        <Grid.ColumnDefinitions>
                                            <!-- Left Icon-->
                                            <ColumnDefinition Width="Auto"/>
                                            <!-- Title -->
                                            <ColumnDefinition Width="*"/>
                                            <!-- Profile -->
                                            <ColumnDefinition Width="Auto"/>
                                            <!-- Settings -->
                                            <ColumnDefinition Width="Auto"/>
                                            <!-- Window Buttons -->
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        
                                        <!-- Left Icon-->
                                        <Button Style="{StaticResource SystemIconButton}" Command="{Binding MenuCommand}">
                                            <Image Source="/src/Icons/033-laptop-1.png"/>
                                        </Button>
                                        <!-- Title Text-->
                                        <Viewbox Grid.Column="1" Margin="0">
                                            <TextBlock Style="{StaticResource HeaderText}" Text="{Binding RelativeSource={RelativeSource TemplatedParent}, Path=Title, FallbackValue='Project Atlantis'}" />
                                        </Viewbox>
                                        <!-- Profile Button -->
                                        <Button Grid.Column="2" Style="{StaticResource HeaderMenuButton}">
                                            <TextBlock Style="{StaticResource HeaderText}" Text="Profile" />
                                        </Button>
                                        <!-- Settings Button -->
                                        <Button Grid.Column="3" Style="{StaticResource HeaderMenuButton}">
                                            <TextBlock Style="{StaticResource HeaderText}" Text="Settings" />
                                        </Button>
                                        <!-- Window Buttons -->
                                        <StackPanel Grid.Column="4" Orientation="Horizontal" >
                                            <Button Style="{StaticResource WindowControlButton}" Content="_" />
                                            <Button Style="{StaticResource WindowControlButton}" Content="[ ]" />
                                            <Button Style="{StaticResource WindowControlButton}" Content="X" />
                                        </StackPanel>
                                        
                                    </Grid>
                                    
                                </Grid>

                            </Grid>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    
    <WindowChrome.WindowChrome >
        <WindowChrome 
            ResizeBorderThickness="{Binding ResizeBorderThickness}"
            CaptionHeight="{Binding TitleHeight}"
            GlassFrameThickness="0"
            />
    </WindowChrome.WindowChrome>
    <Grid>

    </Grid>
</Window>
